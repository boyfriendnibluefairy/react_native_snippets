/** @type {import('next').NextConfig} */
const nextConfig = {
    // GMO this is added to make sure that all href links exists
    // during coding
    experimental: {
        typedRoutes: true
    },
};

export default nextConfig;
